# Процесс разработки
# ООП
`Warrior` и `Worker` для ООП добавляются как наследники класса `Unit`.

Было принятно решение сделать метод `act()` виртуальным, тогда и класс `Unit` становится абстрактным. Для этого необходимо:
1. Изменить `unit.h`, сделав метод `act()` виртуальным. Также необходимо добавить виртуальный деструктор;
2. Изменить `unit.cpp`, убрав метод `act()`.

Теперь можно добавить Warrior и Worker. Для этого:

3. Создам заголовок `warrior.h`;
4. Создам класс `warrior.cpp`;
5. Создам заголовок `worker.h`;
6. Создам класс `worker.cpp`.

Далее пришлось модифицировать племя, а именно `tribe.h` и `tribe.cpp`:

7. В прошлой стадии не продумал хранение сущностей в виде указателей. Пришлось исправить в `tribe.h`, теперь вместо `Unit` в племени хранятся `std::unique_ptr<Unit>`;
8. Добавил метод `void addUnit(std::unique_ptr<Unit> unit)` в `tribe.h` для добавления сущностей в племя
9. Обновил конструктор в `tribe.h`
10. Обновил конструктор в `tribe.c`
11. Добавил реализацию метода `addUnit`
12. Исправил метод `actAll` для работы с указателями

Далее пришлось модифицировать `simulation.h` и `simulation.cpp`:

13. В `simulation.h` был добавлен метод `createUnits()` для случайной генерации типов сущностей 
14. В `simulation.h` добавил поле `_unit_count`
15. В `simulation.cpp` обновил конструктор для `_unit_count`
16. В `simulation.cpp` добавил реализацию `createUnits()`

17. В `main.cpp` добавил вызов функции `createUnits()`

# ППП
`Warrior` и `Worker` для ППП добавляются как специализации структуры `Unit`, которая теперь является обобщением.

1. В `unit.h` сделал из структуры обощение
2. Добавил специализации `worker` и `warrior`
3. В `unit.c` очистил тело функции `unit_act`

Перехожу к работе над специализациями:

4. Создал заголовок `warrior.h`;
5. Создал класс `warrior.c`;
6. Создал заголовок `worker.h`;
7. Создал класс `worker.c`.

8. В `tribe.h` была добавлена функция `tribe_add` для добавления сущностей в племя(аналогично с ООП)
9. В `tribe.c` реализовал функцию `tribe_add`
10. В `tribe.c` добавил обновление счётчика `unit_count`, так как теперь сущности добавляются внутри `tribe`(также можно перейти ко встроенной в ppVector функции `size`)
11. В прошлой стадии допустил ошибку - правильнее хранить в векторе указатели на `Unit`, так что пришлось изменить функцию `tribe_act_all` для работы с указателями

12. Также был исправлен `container.h` для работы с указателями

13. В `simulation.h` был добавлен счётчик `unit_count`(аналогично с ООП)
14. В `simulation.h` была добавлена функция `simulation_add_units` для случайной генерации типов сущностей
15. В `simulation.c` обновил конструктор для `unit_count`
16. В `simulation.c` добавил реализацию для `simulation_add_units`

17. Добавил в `main.c` вызов функции `simulation_add_units` 